'use client';

// Cursor glow removed — it caused continuous mousemove listeners and spring physics
// that degraded scroll performance, especially on lower-end hardware.
export default function CursorGlow() {
  return null;
}

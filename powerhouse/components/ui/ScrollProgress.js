'use client';

// Removed spring-based scroll progress tracking — replaced with CSS-only approach
// The spring useScroll hook on every scroll event was causing jank
export default function ScrollProgress() {
  return null;
}
